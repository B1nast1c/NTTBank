package project.infrastructure.adapters;

class CurrAccAdapterTest {
  /*@Mock
  CurrAccRepo currentRepo;
  @Mock
  SaveDomainValidations domainValidations;
  @Mock
  WebClientSrv clientService;
  @Mock
  UpdateDomainValidations updateDomainValidations;
  @Mock
  ReactiveMongoTemplate reactiveMongoTemplate;
  @InjectMocks
  CurrAccAdapter currAccAdapter;

  @BeforeEach
  void setUp() {
    MockitoAnnotations.openMocks(this);
  }

  @Test
  void testSave() {
    when(domainValidations.validateCurrentAccount(any(Client.class), any(CurrAccDTO.class))).thenReturn(null);
    when(clientService.getClientByiD(anyString())).thenReturn(null);

    Mono<Object> result = currAccAdapter.save("bankAccountDTO", new Client("customId", "clientType", "clientName", "clientAddress", "clientEmail", "clientPhone", "documentNumber", Boolean.TRUE));
    Assertions.assertNull(result);
  }

  @Test
  void testUpdate() {
    when(updateDomainValidations.validateAmmount(any(Object.class))).thenReturn(null);
    when(reactiveMongoTemplate.findAndModify(any(Query.class), any(UpdateDefinition.class), any(Class<T>.class))).thenReturn(null);

    Mono<Object> result = currAccAdapter.update("foundAccount", "updatedAccount");
    Assertions.assertNull(result);
  }*/
}